namespace Platformer.Core
{
    public enum Layers
    {
        defaultLayer = 0,
        transpanentFX = 1,
        IgnoreRaycast = 2,
        enemy = 3,
        water = 4,
        ui = 5,
        hookPoints = 6
    }
}